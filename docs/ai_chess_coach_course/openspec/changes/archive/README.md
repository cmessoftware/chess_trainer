# OpenSpec Archive

This folder stores completed changes for `ai_chess_coach_course`.

## Archiving Rule
- Move a change here only when `proposal.md`, `design.md`, `tasks.md`, and related `specs/` updates are complete.
- Keep original change folder name prefixed with completion date: `YYYY-MM-DD-<change-name>`.

## Traceability
- Do not edit archived content except for critical typo fixes.
- Keep each archived change self-contained for auditability.
